name: 'toJSON'
description: 'Convert Yaml to JSON.'
author: 'phlax'
inputs:
  yaml:
    type: string
    description: >
      Yaml string to convert.
  compact:
    type: boolean
    default: true
    description: >
      Compact JSON output.

runs:
  using: 'node16'
  main: 'dist/index.js'
