name: 'Ifelse'
description: 'Ternary operator.'
author: 'phlax'
inputs:
  input:
    description: >
      File paths to hash.
    default: ""
  default:
    description: >
      File paths to hash.
  elif:
    description: >
      Fail if the resulting hash is empty.
runs:
  using: 'node16'
  main: 'dist/index.js'
