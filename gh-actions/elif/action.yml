name: 'Ifelse'
description: 'Ternary operator.'
author: 'phlax'
inputs:
  input:
    description: >
      File paths to hash.
    default: ""
  default:
    description: >
      File paths to hash.
  elif:
    description: >
      Fail if the resulting hash is empty.
runs:
  using: node20
  main: 'dist/index.js'
