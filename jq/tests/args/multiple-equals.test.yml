name: args::parse multiple equals signs in value

input: "cmd --config=key1=value1"

expression: 'args::parse({"flags": [], "options": ["config"]})'

imports:
- args

expected:
  args:
  - "cmd"
  flags: {}
  options:
    config: "key1=value1"
