name: args::parse URL with multiple equals signs in value

input: "--url=https://example.com/path?key=value"

expression: 'args::parse({"flags": [], "options": ["url"]})'

imports:
- args

expected:
  args: []
  flags: {}
  options:
    url: "https://example.com/path?key=value"
