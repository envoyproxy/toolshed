name: args::parse repeated short option with space-separated values

input: "cmd -u this -u that"

expression: 'args::parse({"options": ["u"]})'

imports:
- args

expected:
  args:
  - "cmd"
  flags: {}
  options:
    u:
    - "this"
    - "that"
