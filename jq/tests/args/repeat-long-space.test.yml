name: args::parse repeated long option with space-separated values

input: "--update foo --update bar"

expression: 'args::parse({"options": {"update": "array"}})'

imports:
- args

expected:
  args: []
  flags: {}
  options:
    update:
    - "foo"
    - "bar"
