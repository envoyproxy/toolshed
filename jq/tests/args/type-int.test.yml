name: args::parse int type conversion

input: "--count=5 --max 100"

expression: 'args::parse({"options": {"count": "int", "max": "int"}})'

imports:
- args

expected:
  args: []
  flags: {}
  options:
    count: 5
    max: 100
