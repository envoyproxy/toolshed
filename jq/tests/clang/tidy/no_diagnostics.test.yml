name: clang/tidy::parse returns an empty list when no diagnostics are present

raw: true

module: clang/tidy::parse

input: |
  # Empty clang-tidy output (no diagnostics)
  0 warnings generated.

expected: []
