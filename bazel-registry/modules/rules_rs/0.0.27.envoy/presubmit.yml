bcr_test_module:
  module_path: ""
  matrix:
    platform:
    - debian11
    - ubuntu2404
    bazel:
    - 8.x
  tasks:
    run_tests:
      name: "Verify build targets"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_targets:
      - "@rules_rs//..."
