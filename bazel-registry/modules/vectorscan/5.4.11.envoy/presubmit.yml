matrix:
  platform:
  - debian11
  - ubuntu2004
  - ubuntu2204
  bazel:
  - 8.x
  - 7.x
tasks:
  verify_targets:
    name: Build vectorscan
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
    - '@vectorscan//:vectorscan'
