matrix:
  platform:
  - ubuntu2004
  - macos
  - windows
tasks:
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    build_targets:
    - '@googleurl//url:url'
    build_flags:
    - '--cxxopt=-std=c++14'
    - '--cxxopt=-fno-strict-aliasing'
