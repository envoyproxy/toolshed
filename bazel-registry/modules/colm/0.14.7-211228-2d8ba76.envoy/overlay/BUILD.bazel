# Install-like layout for rules_foreign_cc consumers
genrule(
    name = "colm_install",
    srcs = [":colm"],
    outs = ["bin/colm"],
    cmd = "mkdir -p $$(dirname $@) && cp $< $@",
    visibility = ["//visibility:public"],
)