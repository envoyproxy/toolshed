matrix:
  platform:
  - debian11
  - macos
  - ubuntu2404
  bazel:
  - 8.x

tasks:
  verify_targets:
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
    - "@toolchains_llvm//..."
