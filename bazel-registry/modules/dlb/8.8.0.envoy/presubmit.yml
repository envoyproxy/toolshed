matrix:
  platform:
  - debian11
  - ubuntu2204
  bazel:
  - 8.x
  - 7.x
tasks:
  verify_targets:
    name: Build dlb library
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
    - '@dlb//:dlb'
