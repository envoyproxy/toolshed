matrix:
  platform:
  - debian11
  - ubuntu2204
  - macos
  bazel:
  - 8.x
  - 7.x
tasks:
  verify_targets:
    name: Verify xscan-fat-runtime module loads
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
    - '@xscan-fat-runtime//:fat_runtime.bzl'
