matrix:
  platform:
  - debian11
  - ubuntu2204
  - macos
  - macos_arm64
  bazel:
  - 8.x
  - 7.x
tasks:
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
    - '@aws-c-auth-testdata//:sigv4_tests'
    - '@aws-c-auth-testdata//:sigv4a_tests'
