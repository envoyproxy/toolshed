matrix:
  bazel:
  - 7.x
  - 8.x

tasks:
  verify_targets:
    name: Verify build targets
    platform: macos_arm64
    bazel: ${{ bazel }}
    build_targets:
    - '@rules_apple//apple:apple'
    build_flags:
    - "--repo_env=BAZEL_DO_NOT_DETECT_CPP_TOOLCHAIN=1"
