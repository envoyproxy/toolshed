matrix:
  bazel:
  - 7.x
  - 8.x

tasks:
  verify_targets:
    name: Verify build targets
    platform: macos_arm64
    bazel: ${{ bazel }}
    build_targets:
    - '@com_github_buildbuddy_io_rules_xcodeproj//xcodeproj:xcodeproj'
