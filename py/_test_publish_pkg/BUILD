# Test fixture — NOT a real publishable package.
# This BUILD file uses toolshed_package so the fixture goes through
# the same publish pipeline as real packages.

toolshed_package("_test_publish_pkg")
