name: Github/actions

on:
  push:
  pull_request:


jobs:
  gh-actions:
    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: appauth
          - name: dispatch
          - name: github/mutex
          - name: hashfiles
          - name: jq
          - name: retest
          - name: torun
          - name: status
    steps:
    - uses: actions/checkout@v4
    - name: Use Node.js ${{ matrix.node-version || '20.x' }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version || '20.x' }}
    - run: cd gh-actions/${{ matrix.name }} && npm install
    - run: cd gh-actions/${{ matrix.name }} && npm run lint
      if: ${{ matrix.name != 'status' }}
    - run: cd gh-actions/${{ matrix.name }} && npm test
      if: ${{ matrix.name != 'status' }}
