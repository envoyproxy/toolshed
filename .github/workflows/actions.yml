name: Github actions

on:
  push:
  pull_request:

jobs:
  appauth:
    runs-on: ubuntu-22.04
    strategy:
      matrix:
        node-version:
        - 16.x
    steps:
    - uses: actions/checkout@v4
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
    - run: cd gh-actions/appauth && npm install
    - run: cd gh-actions/appauth && npm run lint
    - run: cd gh-actions/appauth && npm test

  dispatch:
    runs-on: ubuntu-22.04
    strategy:
      matrix:
        node-version:
        - 16.x
    steps:
    - uses: actions/checkout@v4
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
    - run: cd gh-actions/dispatch && npm install
    - run: cd gh-actions/dispatch && npm run lint
    - run: cd gh-actions/dispatch && npm test

  elif:
    runs-on: ubuntu-22.04
    strategy:
      matrix:
        node-version:
        - 16.x
    steps:
    - uses: actions/checkout@v4
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
    - run: cd gh-actions/elif && npm install
    - run: cd gh-actions/elif && npm run lint
    - run: cd gh-actions/elif && npm test

  hashfiles:
    runs-on: ubuntu-22.04
    strategy:
      matrix:
        node-version:
        - 16.x
    steps:
    - uses: actions/checkout@v4
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
    - run: cd gh-actions/hashfiles && npm install
    - run: cd gh-actions/hashfiles && npm run lint
    - run: cd gh-actions/hashfiles && npm test

  jq:
    runs-on: ubuntu-22.04
    strategy:
      matrix:
        node-version:
        - 16.x
    steps:
    - uses: actions/checkout@v4
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
    - run: cd gh-actions/jq && npm install
    - run: cd gh-actions/jq && npm run lint
    - run: cd gh-actions/jq && npm test

  retest:
    runs-on: ubuntu-22.04
    strategy:
      matrix:
        node-version:
        - 16.x
    steps:
    - uses: actions/checkout@v4
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
    - run: cd gh-actions/retest && npm install
    - run: cd gh-actions/retest && npm run lint
    - run: cd gh-actions/retest && npm test

  status:
    runs-on: ubuntu-22.04
    needs: []
    strategy:
      matrix:
        node-version:
        - 16.x
    steps:
    - uses: actions/checkout@v4
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
    - run: cd gh-actions/retest && npm install
    # - run: cd gh-actions/retest && npm run lint
    - run: cd gh-actions/retest && npm test

  torun:
    runs-on: ubuntu-22.04
    needs: []
    strategy:
      matrix:
        node-version:
        - 16.x
    steps:
    - uses: actions/checkout@v4
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
    - run: cd gh-actions/retest && npm install
    - run: cd gh-actions/retest && npm run lint
    - run: cd gh-actions/retest && npm test

  indent:
    runs-on: ubuntu-22.04
    strategy:
      matrix:
        node-version:
        - 16.x
    steps:
    - uses: actions/checkout@v4
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
    - run: cd gh-actions/str/indent && npm install
    - run: cd gh-actions/str/indent && npm run lint
    - run: cd gh-actions/str/indent && npm test

  github_mutex:
    runs-on: ubuntu-22.04
    needs: []
    strategy:
      matrix:
        node-version:
        - 16.x
    steps:
    - uses: actions/checkout@v4
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
    - run: cd gh-actions/github/mutex && npm install
    - run: cd gh-actions/github/mutex && npm run lint
    - run: cd gh-actions/github/mutex && npm test
