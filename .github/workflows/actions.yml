name: Github/actions CI

on:
  push:
    branches:
    - main
  pull_request:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true


jobs:
  request:
    runs-on: ubuntu-24.04
    outputs:
      run: ${{ steps.filter.outputs.run }}
      tests: ${{ steps.tests.outputs.output }}
    steps:
    - uses: envoyproxy/toolshed/actions/github/should-run@3d5bb981268f22161fa1c0c87bfc6828887c4e0e  # actions-v0.4.22
      id: filter
      with:
        config: |
          paths:
          - 'actions/**'
          - 'js/**'
          - '.github/workflows/actions.yml'
    - id: tests
      uses: envoyproxy/toolshed/actions/bson@3d5bb981268f22161fa1c0c87bfc6828887c4e0e  # actions-v0.4.22
      with:
        input: |
          OUTPUT="$(find actions -path "*/tests/*.test.yml" -type f)"
        filter: |
          bash::output
        result-filter: |
          split("\n")
          | map(select(length > 0))

  actions:
    runs-on: ubuntu-24.04
    needs: request
    if: >-
      fromJSON(needs.request.outputs.run)
      && needs.request.outputs.tests != '[]'
    strategy:
      fail-fast: false
      matrix:
        test: ${{ fromJSON(needs.request.outputs.tests) }}
    steps:
    - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
    - name: Run test
      uses: envoyproxy/toolshed/actions/test-runner@3d5bb981268f22161fa1c0c87bfc6828887c4e0e  # actions-v0.4.22
      with:
        config: ${{ matrix.test }}
        name: ${{ matrix.test }}

  status:
    runs-on: ubuntu-24.04
    if: >-
      always()
      && github.event_name == 'pull_request'
    name: Actions
    needs:
    - request
    - actions
    steps:
    - run: |
        if [[ "${{ contains(needs.*.result, 'failure') }}" == "true" || "${{ contains(needs.*.result, 'cancelled') }}" == "true" ]]; then
            echo "One or more jobs failed or were cancelled"
            exit 1
        fi
        echo "All required jobs passed or were skipped"
