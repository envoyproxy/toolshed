name: Docker CI

on:
  push:
    branches:
    - main
  pull_request:
    branches:
    - main

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  request:
    runs-on: ubuntu-24.04
    outputs:
      run: ${{ steps.filter.outputs.run }}
    steps:
    - uses: envoyproxy/toolshed/actions/github/should-run@094549c1d217c1d0b28b0570431c71e8602dc0f0
      id: filter
      with:
        config: |
          paths:
          - 'docker/build/**'
          - '.github/workflows/docker.yml'
          - '.github/docker-manifests.yml'

  build:
    runs-on: ${{ matrix.arch == 'amd64' && 'ubuntu-24.04' || 'ubuntu-24.04-arm' }}
    needs: request
    if: >-
      github.repository_owner == 'envoyproxy'
      && fromJSON(needs.request.outputs.run || 'false')
    strategy:
      fail-fast: false
      matrix:
        arch:
        - amd64
        - arm64
    name: build (${{ matrix.arch }})
    steps:
    - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@4d04d5d9486b7bd6fa91e7baf45bbb4f8b9deedd  # v4.0.0

    - name: Get docker SHA
      id: docker-sha
      shell: bash
      run: |
        TAG_SHA=$(git log -1 --pretty=format:"%H" ./docker/build)
        echo "sha=${TAG_SHA}" >> $GITHUB_OUTPUT

    - name: Build Docker image (debian-${{ matrix.arch }})
      run: |
        cd docker/build
        export OS_FAMILY=linux
        export OS_DISTRO=debian
        export CONTAINER_SHA=${{ steps.docker-sha.outputs.sha }}
        export CONTAINER_TAG=${{ steps.docker-sha.outputs.sha }}
        export BUILD_TOOLS_PLATFORMS=${{ matrix.arch == 'amd64' && 'linux/amd64' || 'linux/arm64' }}
        export SAVE_OCI=true
        export LOAD_IMAGE=true
        ./push.sh
        echo "::group::Docker images"
        docker images | grep envoy-build || echo "No envoy-build images found"
        echo "::endgroup::"
      shell: bash

    - name: Upload OCI artifacts
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
      with:
        name: oci-debian-${{ matrix.arch }}
        path: docker/build/oci-output/*.tar
        retention-days: 1
        if-no-files-found: error

    - name: Test Docker image
      run: |
        IMAGE_TAG="envoyproxy/envoy-build:ci-${{ steps.docker-sha.outputs.sha }}-${{ matrix.arch }}"
        echo "Testing main image: $IMAGE_TAG"
        echo "Attempting to run container and read build-id..."
        if BUILD_ID=$(docker run --rm --entrypoint /bin/cat "$IMAGE_TAG" /.build-id 2>&1); then
          echo "Build ID: $BUILD_ID"
          EXPECTED_ID="ci-${{ steps.docker-sha.outputs.sha }}"
          if [[ "$BUILD_ID" == "$EXPECTED_ID" ]]; then
            echo "✓ Build ID is correct"
          else
            echo "✗ Build ID mismatch: expected '$EXPECTED_ID', got '$BUILD_ID'"
            exit 1
          fi
        else
          echo "ERROR: Failed to run container or read build-id"
          echo "Docker run output: $BUILD_ID"
          echo "Checking if image exists:"
          docker images | grep -E "(REPOSITORY|envoy-build.*ci-)"
          echo "Trying docker inspect:"
          docker image inspect "$IMAGE_TAG" 2>&1 | head -20 || echo "Inspect failed"
          exit 1
        fi
      shell: bash

  push-manifests:
    name: Push manifests (${{ github.event_name == 'pull_request' && 'dry run' || 'push' }})
    runs-on: ubuntu-24.04
    needs:
    - request
    - build
    if: >-
      always()
      && github.repository_owner == 'envoyproxy'
      && fromJSON(needs.request.outputs.run || 'false')
      && needs.build.result == 'success'
    permissions:
      contents: read
      packages: write
    steps:
    - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

    - name: Get docker SHA
      id: docker-sha
      shell: bash
      run: |
        TAG_SHA=$(git log -1 --pretty=format:"%H" ./docker/build)
        echo "sha=${TAG_SHA}" >> $GITHUB_OUTPUT

    - name: Generate manifest configuration
      id: manifest-config
      uses: envoyproxy/toolshed/actions/jq@094549c1d217c1d0b28b0570431c71e8602dc0f0
      with:
        input: .github/docker-manifests.yml
        input-format: yaml-path
        filter: >-
          .manifests
          | map(. + {
              tag: (.tag | gsub("{sha}"; "${{ steps.docker-sha.outputs.sha }}")),
              "artifact-pattern": (.["artifact-pattern"] | gsub("{sha}"; "${{ steps.docker-sha.outputs.sha }}")),
              "additional-tags": (if .["additional-tags"] then .["additional-tags"] | map(gsub("{sha}"; "${{ steps.docker-sha.outputs.sha }}")) else null end)
            })
          | {manifests: .}

    - name: Collect and push OCI artifacts
      uses: envoyproxy/toolshed/actions/oci/collector@094549c1d217c1d0b28b0570431c71e8602dc0f0
      with:
        manifest-config: ${{ steps.manifest-config.outputs.value }}
        dry-run: ${{ github.event_name == 'pull_request' }}
        dockerhub-username: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' && secrets.DOCKERHUB_USERNAME || '' }}
        dockerhub-password: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' && secrets.DOCKERHUB_PASSWORD || '' }}

  status:
    runs-on: ubuntu-24.04
    if: >-
      always()
      && github.event_name == 'pull_request'
    name: Docker
    needs:
    - request
    - build
    - push-manifests
    steps:
    - run: |
        if [[ "${{ contains(needs.*.result, 'failure') }}" == "true" || "${{ contains(needs.*.result, 'cancelled') }}" == "true" ]]; then
            echo "One or more jobs failed or were cancelled"
            exit 1
        fi
        echo "All required jobs passed or were skipped"
