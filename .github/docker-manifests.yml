manifests:
# worker
- name: envoy-build
  tag: worker-{version}
  registry: docker.io/envoyproxy
  architectures:
  - amd64
  - arm64
  artifact-pattern: oci-debian-{arch}/debian-worker-{version}-{arch}.tar
# worker (gcr)
- name: envoy-build
  tag: worker-{version}
  registry: gcr.io/envoy-ci
  architectures:
  - amd64
  - arm64
  artifact-pattern: oci-debian-{arch}/debian-worker-{version}-{arch}.tar
# ci
- name: envoy-build
  tag: ci-{version}
  registry: docker.io/envoyproxy
  architectures:
  - amd64
  - arm64
  artifact-pattern: oci-debian-{arch}/debian-ci-{version}-{arch}.tar
# gcc
- name: envoy-build
  tag: gcc-{version}
  registry: docker.io/envoyproxy
  architectures:
  - amd64
  - arm64
  artifact-pattern: oci-debian-{arch}/debian-gcc-{version}-{arch}.tar
# gcc (gcr)
- name: envoy-build
  tag: gcc-{version}
  registry: gcr.io/envoy-ci
  architectures:
  - amd64
  - arm64
  artifact-pattern: oci-debian-{arch}/debian-gcc-{version}-{arch}.tar
# devtools
- name: envoy-build
  tag: devtools-{version}
  registry: docker.io/envoyproxy
  architectures:
  - amd64
  - arm64
  artifact-pattern: oci-debian-{arch}/debian-devtools-{version}-{arch}.tar
# docker
- name: envoy-build
  tag: docker-{version}
  registry: docker.io/envoyproxy
  architectures:
  - amd64
  - arm64
  artifact-pattern: oci-debian-{arch}/debian-docker-{version}-{arch}.tar
# test
- name: envoy-build
  tag: test-{version}
  registry: docker.io/envoyproxy
  architectures:
  - amd64
  - arm64
  artifact-pattern: oci-debian-{arch}/debian-test-{version}-{arch}.tar
# mobile (amd64 only)
- name: envoy-build
  tag: mobile-{version}
  registry: docker.io/envoyproxy
  architectures:
  - amd64
  artifact-pattern: oci-debian-{arch}/debian-mobile-{version}-{arch}.tar
